language: cpp
sudo: required
dist: bionic
compiler:
  - gcc
os:
  - linux

install:
  - sudo apt-get install qt5-default qt5-qmake

script:
  - qmake qt-todo.pro test=1
  - make
  - ./testing/qt-todo

